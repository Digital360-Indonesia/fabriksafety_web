---
// Video Section Component
---

<section class="relative bg-gray-900 py-24 md:py-32 overflow-hidden">
  <!-- Video Background -->
  <div class="absolute inset-0 z-0">
    <div class="w-full h-full bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900">
      <!-- Placeholder for video background -->
      <img
        src="https://images.unsplash.com/photo-1504307651254-35680f356dfd?w=1920&h=1080&fit=crop"
        alt="Production facility"
        class="w-full h-full object-cover opacity-40"
      />
    </div>
  </div>

  <!-- Overlay -->
  <div class="absolute inset-0 bg-gradient-to-r from-gray-900/95 via-gray-900/80 to-transparent z-10"></div>

  <div class="container-custom relative z-20">
    <div class="max-w-2xl">
      <div class="space-y-6">
        <div class="inline-flex items-center space-x-2 bg-primary-600/20 border border-primary-500/30 rounded-full px-4 py-2">
          <span class="w-2 h-2 bg-primary-400 rounded-full animate-pulse"></span>
          <span class="text-primary-300 text-sm font-medium">Production Sendiri - Quality Terjamin</span>
        </div>

        <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold text-white leading-tight">
          Produksi Sendiri dengan Standar Internasional
        </h2>

        <p class="text-lg text-gray-300 max-w-xl">
          Setiap produk Fabrik Safety dibuat dengan quality control ketat dan menggunakan bahan premium untuk memastikan perlindungan maksimal bagi pengguna.
        </p>

        <div class="flex flex-col sm:flex-row gap-4 pt-4">
          <a href="#categories" class="group bg-white text-gray-900 hover:bg-primary-600 hover:text-white px-8 py-4 rounded-lg font-semibold transition-all duration-300 inline-flex items-center justify-center">
            Lihat Produk
            <svg class="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          </a>
          <a href="https://wa.me/62851111204599" class="border-2 border-white text-white hover:bg-white hover:text-gray-900 px-8 py-4 rounded-lg font-semibold transition-all duration-300 inline-flex items-center justify-center">
            <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 24 24">
              <path d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
            </svg>
            Konsultasi Gratis
          </a>
        </div>

        <!-- Stats -->
        <div class="grid grid-cols-3 gap-6 pt-8">
          <div>
            <p class="text-3xl font-bold text-white">500+</p>
            <p class="text-sm text-gray-400">Perusahaan</p>
          </div>
          <div>
            <p class="text-3xl font-bold text-white">100K+</p>
            <p class="text-sm text-gray-400">Unit Terjual</p>
          </div>
          <div>
            <p class="text-3xl font-bold text-white">5+</p>
            <p class="text-sm text-gray-400">Tahun Pengalaman</p>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Play Button (Optional - for video popup) -->
  <div class="absolute right-8 md:right-16 bottom-1/2 transform translate-y-1/2 hidden lg:block">
    <button class="w-20 h-20 bg-white/10 backdrop-blur-sm hover:bg-primary-600 rounded-full flex items-center justify-center transition-all duration-300 group">
      <svg class="w-8 h-8 text-white group-hover:scale-110 transition-transform" fill="currentColor" viewBox="0 0 24 24">
        <path d="M8 5v14l11-7z"/>
      </svg>
    </button>
  </div>
</section>
